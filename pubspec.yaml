name: lazy_load_indexed_stack_plus
description: A package that extends IndexedStack to allow for lazy loading.
version: 1.4.0
homepage: https://github.com/rizkyghofur/lazy_load_indexed_stack_plus
repository: https://github.com/rizkyghofur/lazy_load_indexed_stack_plus

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
